Inference Setup
-----------------------
The entrypoint for inference with DeepScale is ``deepscale.init_inference()``.

Example usage:

.. code-block:: python

    engine = deepscale.init_inference(model=net)

.. autofunction:: deepscale.init_inference
